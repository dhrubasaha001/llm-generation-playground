# LLM Generation Playground

This project demonstrates how GPT-style models generate text
token by token.

Concepts explored:
- Tokenization
- Logits
- Greedy decoding
- Temperature sampling
- Autoregressive generation loop

Instead of using model.generate(), this project implements
a custom decoding loop to understand how LLMs produce text.
